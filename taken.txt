

Bicep template (incl. queues)
→ Nog niet gedaan.

Deploy.ps1 script
→ Nog niet gedaan.

Deploy naar Azure en werkend endpoint
→ Nog niet gedaan (nu lokaal werkend).

SAS token i.p.v. public blob
→ Nog niet. Nu direct public blob-URL’s.

CI/CD build & deploy via GitHub
→ Nog niet.

Authenticatie op API
→ Nog niet.

Status opslaan in Table Storage
→ Je hebt status via GetStatusFunction, maar nog niet in Table Storage.